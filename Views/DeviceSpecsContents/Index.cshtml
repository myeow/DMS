@model IEnumerable<DMS.Models.DeviceSpecsContent>

@{
    ViewBag.Title = "Device Specifications";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Devices.DeviceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsCat.DeviceSpecsCatName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsContentVal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsContentDateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsContentCreatedBy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsContentDateModified)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceSpecsContentModifiedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Devices.DeviceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsCat.DeviceSpecsCatName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsContentVal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsContentDateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsContentCreatedBy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsContentDateModified)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceSpecsContentModifiedBy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeviceSpecsContentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeviceSpecsContentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeviceSpecsContentId })
        </td>
    </tr>
}

</table>
